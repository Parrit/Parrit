branches:
  only:
    - master

language: java
jdk:
  - oraclejdk8

install:
  - ( cd frontend; npm install )

before_script:
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start

script:
  - ./gradlew
